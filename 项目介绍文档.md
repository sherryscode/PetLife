# PetLife 宠物生活管理系统 - 项目介绍文档

---

## 一、项目概述

### 1.1 项目背景

PetLife 宠物生活管理系统是一个面向宠物行业的全生命周期管理平台，旨在为宠物主人、宠物店商户和系统管理员提供一站式的宠物管理解决方案。系统涵盖了宠物健康管理、商城购物、预约服务、社区互动等核心功能。

### 1.2 项目架构

系统采用微服务架构，前后端分离设计：

- **后端**: Spring Cloud Alibaba 2023.0.1.0 + Spring Boot 3.2.0，包含 7 个微服务模块
- **前端**: Vue 3 + Vite + Pinia + Element Plus，包含 3 个独立应用
- **中间件**: Nacos（服务发现/配置中心）、MySQL（数据存储）、Redis（缓存）

### 1.3 功能模块

| 模块 | 服务 | 核心功能 |
|------|------|---------|
| 认证授权 | petlife-auth | 用户/商户/管理员登录、权限管理、操作日志 |
| 商城服务 | petlife-mall | 商品管理、购物车、订单管理 |
| 订单服务 | petlife-order | 预约订单、门店管理、服务套餐 |
| 健康服务 | petlife-health | 宠物档案、疫苗记录、体检记录、病例管理 |
| 社区服务 | petlife-social | 社区帖子、评论、寻宠启事 |
| 消息服务 | petlife-message | 消息推送、定时任务提醒 |
| 统一网关 | petlife-gateway | 路由转发、鉴权过滤、跨域处理 |

---

## 二、核心功能实现方法

### 2.1 认证授权模块

#### 2.1.1 JWT 无状态认证

**实现思路**: 采用 JWT（JSON Web Token）实现无状态认证，用户登录成功后生成 Token，后续请求携带 Token 进行身份验证。

**关键代码**:

```java
// JwtUtil.java - 生成Token
public static String generateToken(Map<String, Object> claims) {
    return Jwts.builder()
            .claims(claims)
            .expiration(new Date(System.currentTimeMillis() + EXPIRATION_TIME))
            .signWith(getSigningKey())
            .compact();
}

// AdminController.java - 登录接口
@PostMapping("/login")
public Result<Map<String, Object>> login(@RequestBody Map<String, String> params) {
    Admin admin = adminService.login(username, password);
    
    Map<String, Object> claims = new HashMap<>();
    claims.put("userId", admin.getId());
    claims.put("role", "admin");
    
    String token = JwtUtil.generateToken(claims);
    // 返回token和用户信息
}
```

**认证流程**:
1. 用户登录 → 验证密码 → 生成 Token
2. 前端保存 Token 到 localStorage
3. 后续请求通过 Axios 拦截器自动携带 `Authorization: Bearer {token}`
4. 网关过滤器验证 Token 有效性
5. 验证通过后提取 userId 和 role 传递给后端服务

#### 2.1.2 BCrypt 密码加密

**实现思路**: 使用 BCryptPasswordEncoder 对密码进行加密存储，登录时通过 `matches` 方法验证密码。

**关键代码**:

```java
// 注册时加密密码
admin.setPassword(passwordEncoder.encode(password));

// 登录时验证密码
boolean matches = passwordEncoder.matches(rawPassword, encodedPassword);
```

#### 2.1.3 AOP 操作日志

**实现思路**: 通过 AOP 切面自动记录管理员操作日志，无需在业务代码中手动添加日志记录逻辑。

**关键代码**:

```java
@Aspect
@Component
public class OperationLogAspect {
    @Around("@annotation(com.petlife.common.annotation.OperationLog)")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        // 记录开始时间
        long startTime = System.currentTimeMillis();
        
        // 获取请求信息和用户信息
        // 获取注解信息（module、operation、description）
        
        // 执行目标方法
        Object result = joinPoint.proceed();
        
        // 记录执行时间和结果
        long executionTime = System.currentTimeMillis() - startTime;
        
        // 保存日志到数据库
        SysLog sysLog = new SysLog();
        sysLog.setUserId(userId);
        sysLog.setOperation(operation);
        sysLog.setModule(module);
        sysLog.setExecutionTime(executionTime);
        sysLogService.save(sysLog);
        
        return result;
    }
}
```

**使用方式**:

```java
@PostMapping("/login")
@OperationLog(module = "系统管理", operation = "登录", description = "管理员登录")
public Result<Map<String, Object>> login(...) { }
```

---

### 2.2 商城服务模块

#### 2.2.1 商品管理

**实现思路**: 商品由商户创建，需要经过管理员审核才能上架销售。

**商品状态流转**:
```
创建(auditStatus=0) → 审核通过(auditStatus=1) → 上架(status=1) → 销售中
                                      ↓
                              审核拒绝(auditStatus=2) → 重新修改
```

**关键代码**:

```java
// GoodsController.java - 创建商品
@PostMapping
public Result<Goods> create(@RequestBody Goods goods) {
    goods.setAuditStatus(0);  // 默认待审核
    goods.setSales(0);
    goodsService.save(goods);
    return Result.success("创建成功，等待审核", goods);
}

// GoodsController.java - 审核商品
@PutMapping("/{id}/audit")
public Result<Goods> audit(@PathVariable("id") Long id, @RequestBody Map<String, Integer> params) {
    Goods goods = goodsService.getById(id);
    goods.setAuditStatus(params.get("auditStatus"));
    goodsService.updateById(goods);
    return Result.success("审核完成", goods);
}
```

#### 2.2.2 购物车功能

**实现思路**: 购物车采用 Redis 存储，支持添加商品、修改数量、删除商品等操作。

**购物车数据结构**:
```
key: cart:{userId}
value: Map<goodsId, count>
```

#### 2.2.3 订单创建

**实现思路**: 用户提交订单时，系统生成唯一订单号，扣减商品库存，创建订单记录。

**订单号生成规则**:
```
DD{yyMMdd}{HHmmss}{6位随机数}
例如: DD20240101120000123456
```

---

### 2.3 订单服务模块

#### 2.3.1 预约订单

**实现思路**: 用户选择门店和服务套餐，预约具体时间，商户接单后提供服务。

**订单状态流转**:
```
待接单(0) → 已接单(1) → 服务中(2) → 已完成(3)
           ↓                    ↓
      已取消(4)           已取消(4)
```

**关键代码**:

```java
// ReserveOrderController.java - 更新订单状态
@PutMapping("/{id}/status")
public Result<ReserveOrder> updateStatus(@PathVariable("id") Long id, @RequestBody Map<String, Integer> params) {
    ReserveOrder order = reserveOrderService.getById(id);
    order.setOrderStatus(params.get("orderStatus"));
    reserveOrderService.updateById(order);
    return Result.success("状态更新成功", order);
}
```

#### 2.3.2 门店管理

**实现思路**: 商户可以创建和管理门店信息，每个门店可以提供多种服务套餐。

**门店与服务套餐关系**: 一对多关系，一个门店可以有多个服务套餐。

---

### 2.4 健康服务模块

#### 2.4.1 宠物档案管理

**实现思路**: 用户可以添加宠物信息，包括宠物名称、品种、年龄、性别、体重等基本信息。

**用户与宠物关系**: 一对多关系，一个用户可以拥有多只宠物。

#### 2.4.2 疫苗记录管理

**实现思路**: 记录宠物的疫苗接种情况，包括疫苗名称、接种日期、有效截止日期等。

**关键代码**:

```java
// PetVaccineController.java - 查询宠物疫苗记录
@GetMapping("/pet/{petId}")
public Result<List<PetVaccine>> getByPetId(@PathVariable("petId") Long petId) {
    List<PetVaccine> vaccines = petVaccineService.list(
        new LambdaQueryWrapper<PetVaccine>()
            .eq(PetVaccine::getPetId, petId)
    );
    return Result.success(vaccines);
}
```

#### 2.4.3 体检与病例管理

**实现思路**: 记录宠物的体检报告和病例信息，便于跟踪宠物健康状况。

---

### 2.5 社区服务模块

#### 2.5.1 帖子管理

**实现思路**: 用户可以发布社区帖子，帖子需要经过审核才能展示。支持点赞、收藏、评论等互动功能。

**帖子状态流转**:
```
发布(auditStatus=0) → 审核通过(auditStatus=1) → 展示(status=1)
                        ↓
                审核拒绝(auditStatus=2) → 重新修改
```

**关键代码**:

```java
// CommunityPostController.java - 点赞接口
@PutMapping("/{id}/like")
public Result<CommunityPost> like(@PathVariable("id") Long id, @RequestParam("userId") Long userId) {
    CommunityPost post = communityPostService.likePost(id, userId);
    return Result.success(post);
}
```

#### 2.5.2 评论管理

**实现思路**: 用户可以对帖子进行评论，支持评论列表查询和删除功能。

**帖子与评论关系**: 一对多关系，一个帖子可以有多个评论。

#### 2.5.3 寻宠启事

**实现思路**: 用户可以发布寻宠启事，包括宠物照片、特征描述、丢失地点等信息。

---

### 2.6 消息服务模块

#### 2.6.1 消息推送

**实现思路**: 系统通过消息服务向用户推送通知，包括疫苗到期提醒、预约服务提醒等。

#### 2.6.2 定时任务

**实现思路**: 使用 Spring Scheduler 实现定时任务，每天早上 9 点检查疫苗到期和预约订单情况。

**关键代码**:

```java
// VaccineReminderTask.java
@Scheduled(cron = "0 0 9 * * ?")
public void checkVaccineExpiration() {
    // 调用健康服务获取所有疫苗记录
    Result<?> result = restTemplate.getForObject(
        "http://petlife-health/vaccine/listAll?page=1&size=1000", 
        Result.class
    );
    
    // 检查疫苗有效期，7天内到期发送提醒
    for (vaccine : vaccines) {
        long daysUntilExpiration = ChronoUnit.DAYS.between(LocalDateTime.now(), validUntil);
        if (daysUntilExpiration >= 0 && daysUntilExpiration <= 7) {
            messageService.sendMessage(userId, "疫苗到期提醒", content, 1, "/pet/vaccine");
        }
    }
}
```

---

## 三、数据表设计

### 3.1 基类字段

所有表都继承自 `BaseEntity`，包含以下公共字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键，自增 |
| created_at | DATETIME | 创建时间，自动填充 |
| updated_at | DATETIME | 更新时间，自动填充 |

### 3.2 用户相关表

#### 3.2.1 user（用户表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| phone | VARCHAR(20) | 手机号（登录账号） |
| password | VARCHAR(100) | BCrypt加密密码 |
| nickname | VARCHAR(50) | 用户昵称 |
| avatar | VARCHAR(255) | 头像URL |
| gender | TINYINT | 性别（0-未知，1-男，2-女） |
| address | VARCHAR(255) | 地址 |
| status | TINYINT | 状态（0-禁用，1-正常） |

#### 3.2.2 admin（管理员表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| username | VARCHAR(50) | 用户名（登录账号） |
| password | VARCHAR(100) | BCrypt加密密码 |
| nickname | VARCHAR(50) | 昵称 |
| avatar | VARCHAR(255) | 头像URL |
| status | TINYINT | 状态（0-禁用，1-正常） |

#### 3.2.3 merchant（商户表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| phone | VARCHAR(20) | 手机号（登录账号） |
| password | VARCHAR(100) | BCrypt加密密码 |
| merchant_name | VARCHAR(50) | 商户名称 |
| store_name | VARCHAR(50) | 门店名称 |
| store_address | VARCHAR(255) | 门店地址 |
| store_phone | VARCHAR(20) | 门店电话 |
| business_scope | VARCHAR(255) | 经营范围 |
| business_hours | VARCHAR(100) | 营业时间 |
| rating | DECIMAL(2,1) | 评分 |
| audit_status | TINYINT | 审核状态（0-待审核，1-通过，2-拒绝） |
| status | TINYINT | 状态（0-禁用，1-正常） |

### 3.3 商城相关表

#### 3.3.1 goods（商品表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| merchant_id | BIGINT | 商户ID |
| goods_name | VARCHAR(100) | 商品名称 |
| category | TINYINT | 分类（1-食品，2-用品，3-玩具，4-美容） |
| price | DECIMAL(10,2) | 售价 |
| original_price | DECIMAL(10,2) | 原价 |
| stock | INT | 库存 |
| description | TEXT | 商品描述 |
| images | TEXT | 图片URL（JSON数组） |
| sales | INT | 销量 |
| is_hot | TINYINT | 是否热门（0-否，1-是） |
| audit_status | TINYINT | 审核状态（0-待审核，1-通过，2-拒绝） |
| status | TINYINT | 状态（0-下架，1-上架） |

#### 3.3.2 goods_order（商城订单表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| order_no | VARCHAR(30) | 订单号 |
| user_id | BIGINT | 用户ID |
| total_amount | DECIMAL(10,2) | 总金额 |
| pay_status | TINYINT | 支付状态（0-未支付，1-已支付，2-退款） |
| ship_status | TINYINT | 发货状态（0-未发货，1-已发货，2-已收货） |
| order_status | TINYINT | 订单状态（0-待付款，1-待发货，2-待收货，3-已完成，4-已取消） |
| receiver_name | VARCHAR(50) | 收货人姓名 |
| receiver_phone | VARCHAR(20) | 收货人电话 |
| receiver_address | VARCHAR(255) | 收货地址 |
| remark | VARCHAR(255) | 备注 |

#### 3.3.3 goods_order_item（订单明细表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| order_id | BIGINT | 订单ID |
| goods_id | BIGINT | 商品ID |
| goods_name | VARCHAR(100) | 商品名称 |
| price | DECIMAL(10,2) | 单价 |
| quantity | INT | 数量 |

#### 3.3.4 shop_cart（购物车表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| goods_id | BIGINT | 商品ID |
| quantity | INT | 数量 |
| selected | TINYINT | 是否选中（0-否，1-是） |

### 3.4 订单相关表

#### 3.4.1 reserve_order（预约订单表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| order_no | VARCHAR(30) | 订单号 |
| user_id | BIGINT | 用户ID |
| pet_id | BIGINT | 宠物ID |
| store_id | BIGINT | 门店ID |
| service_id | BIGINT | 服务套餐ID |
| reserve_date | DATE | 预约日期 |
| reserve_time | VARCHAR(20) | 预约时间 |
| total_amount | DECIMAL(10,2) | 总金额 |
| deposit_amount | DECIMAL(10,2) | 定金金额 |
| pay_status | TINYINT | 支付状态（0-未支付，1-已支付，2-退款） |
| order_status | TINYINT | 订单状态（0-待接单，1-已接单，2-服务中，3-已完成，4-已取消） |
| address | VARCHAR(255) | 服务地址 |
| pet_desc | VARCHAR(255) | 宠物描述 |
| remark | VARCHAR(255) | 备注 |

#### 3.4.2 store（门店表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| merchant_id | BIGINT | 商户ID |
| store_name | VARCHAR(50) | 门店名称 |
| store_address | VARCHAR(255) | 门店地址 |
| store_phone | VARCHAR(20) | 门店电话 |
| business_scope | VARCHAR(255) | 经营范围 |
| business_hours | VARCHAR(100) | 营业时间 |
| rating | DECIMAL(2,1) | 评分 |
| status | TINYINT | 状态（0-关闭，1-营业） |

#### 3.4.3 store_service（服务套餐表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| store_id | BIGINT | 门店ID |
| service_name | VARCHAR(100) | 服务名称 |
| service_type | TINYINT | 服务类型 |
| price | DECIMAL(10,2) | 价格 |
| description | VARCHAR(255) | 服务描述 |
| duration | INT | 服务时长（分钟） |
| status | TINYINT | 状态（0-下架，1-上架） |
| sort_order | INT | 排序号 |

### 3.5 健康相关表

#### 3.5.1 pet（宠物表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| pet_name | VARCHAR(50) | 宠物名称 |
| breed | VARCHAR(50) | 品种 |
| age | INT | 年龄（月） |
| gender | TINYINT | 性别（0-未知，1-公，2-母） |
| weight | DECIMAL(5,2) | 体重（kg） |
| avatar | VARCHAR(255) | 头像URL |
| adopt_date | DATE | 领养日期 |
| status | TINYINT | 状态（0-已走失，1-正常） |

#### 3.5.2 pet_vaccine（疫苗记录表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| pet_id | BIGINT | 宠物ID |
| vaccine_name | VARCHAR(100) | 疫苗名称 |
| vaccine_time | DATETIME | 接种时间 |
| valid_until | DATETIME | 有效截止时间 |
| remark | VARCHAR(255) | 备注 |

#### 3.5.3 pet_checkup（体检记录表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| pet_id | BIGINT | 宠物ID |
| hospital_name | VARCHAR(100) | 体检医院名称 |
| checkup_type | VARCHAR(50) | 体检类型 |
| checkup_time | DATETIME | 体检时间 |
| cost | DECIMAL(10,2) | 费用 |
| result | TEXT | 体检结果 |
| remark | VARCHAR(255) | 备注 |

#### 3.5.4 pet_case（病例表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| pet_id | BIGINT | 宠物ID |
| hospital_name | VARCHAR(100) | 就诊医院名称 |
| disease | VARCHAR(100) | 疾病名称 |
| medicine | VARCHAR(255) | 用药信息 |
| diagnosis | TEXT | 诊断结果 |
| visit_time | DATETIME | 就诊时间 |
| cost | DECIMAL(10,2) | 费用 |

### 3.6 社区相关表

#### 3.6.1 community_post（社区帖子表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| title | VARCHAR(100) | 帖子标题 |
| content | TEXT | 帖子内容 |
| images | TEXT | 图片URL（JSON数组） |
| likes | INT | 点赞数 |
| comments | INT | 评论数 |
| favorites | INT | 收藏数 |
| audit_status | TINYINT | 审核状态（0-待审核，1-通过，2-拒绝） |
| status | TINYINT | 状态（0-隐藏，1-展示） |

#### 3.6.2 community_comment（评论表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| post_id | BIGINT | 帖子ID |
| user_id | BIGINT | 用户ID |
| parent_id | BIGINT | 父评论ID（回复评论时使用） |
| content | TEXT | 评论内容 |
| likes | INT | 点赞数 |
| status | TINYINT | 状态（0-隐藏，1-展示） |

#### 3.6.3 pet_lost（寻宠启事表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| pet_name | VARCHAR(50) | 宠物名称 |
| breed | VARCHAR(50) | 品种 |
| gender | TINYINT | 性别 |
| age | INT | 年龄（月） |
| avatar | VARCHAR(255) | 宠物照片 |
| lost_address | VARCHAR(255) | 走失地点 |
| lost_time | DATETIME | 走失时间 |
| contact_phone | VARCHAR(20) | 联系方式 |
| reward | DECIMAL(10,2) | 悬赏金额 |
| description | TEXT | 特征描述 |
| is_top | TINYINT | 是否置顶（0-否，1-是） |
| audit_status | TINYINT | 审核状态（0-待审核，1-通过，2-拒绝） |
| status | TINYINT | 状态（0-寻找中，1-已找到） |

#### 3.6.4 post_like（点赞记录表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| post_id | BIGINT | 帖子ID |
| status | TINYINT | 状态（0-取消点赞，1-已点赞） |

### 3.7 权限相关表

#### 3.7.1 role（角色表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| role_name | VARCHAR(50) | 角色名称 |
| role_code | VARCHAR(50) | 角色编码 |
| description | VARCHAR(255) | 角色描述 |
| status | TINYINT | 状态（0-禁用，1-正常） |

#### 3.7.2 menu（菜单表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| parent_id | BIGINT | 父菜单ID |
| menu_name | VARCHAR(50) | 菜单名称 |
| path | VARCHAR(255) | 路由路径 |
| component | VARCHAR(255) | 组件路径 |
| icon | VARCHAR(50) | 图标 |
| menu_type | TINYINT | 菜单类型（0-目录，1-菜单，2-按钮） |
| sort_order | INT | 排序号 |
| status | TINYINT | 状态（0-禁用，1-正常） |

#### 3.7.3 role_menu（角色菜单关联表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| role_id | BIGINT | 角色ID |
| menu_id | BIGINT | 菜单ID |

#### 3.7.4 user_role（用户角色关联表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| role_id | BIGINT | 角色ID |

### 3.8 系统相关表

#### 3.8.1 sys_log（操作日志表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| user_id | BIGINT | 用户ID |
| user_name | VARCHAR(50) | 用户名称 |
| operation | VARCHAR(50) | 操作类型 |
| module | VARCHAR(50) | 模块名称 |
| description | VARCHAR(255) | 操作描述 |
| url | VARCHAR(255) | 请求URL |
| method | VARCHAR(10) | 请求方法 |
| params | TEXT | 请求参数 |
| result | TEXT | 返回结果 |
| ip | VARCHAR(50) | IP地址 |
| user_agent | VARCHAR(255) | 浏览器信息 |
| execution_time | BIGINT | 执行时间（毫秒） |
| status | TINYINT | 状态（0-失败，1-成功） |
| error_msg | TEXT | 错误信息 |

---

## 四、表关系 ER 图

```
用户(user) ────1:N───→ 宠物(pet)
用户(user) ────1:N───→ 商城订单(goods_order)
用户(user) ────1:N───→ 预约订单(reserve_order)
用户(user) ────1:N───→ 社区帖子(community_post)
用户(user) ────1:N───→ 评论(community_comment)
用户(user) ────1:N───→ 寻宠启事(pet_lost)
用户(user) ────N:N───→ 角色(role) ────N:N───→ 菜单(menu)

商户(merchant) ────1:N───→ 门店(store)
商户(merchant) ────1:N───→ 商品(goods)

门店(store) ────1:N───→ 服务套餐(store_service)
门店(store) ────1:N───→ 预约订单(reserve_order)

宠物(pet) ────1:N───→ 疫苗记录(pet_vaccine)
宠物(pet) ────1:N───→ 体检记录(pet_checkup)
宠物(pet) ────1:N───→ 病例(pet_case)

商城订单(goods_order) ────1:N───→ 订单明细(goods_order_item)

社区帖子(community_post) ────1:N───→ 评论(community_comment)
社区帖子(community_post) ────N:N───→ 用户(user) [点赞]

管理员(admin) ────1:N───→ 操作日志(sys_log)
```

---

## 五、核心设计思想

### 5.1 微服务设计原则

1. **单一职责**: 每个服务只负责一个业务领域
2. **独立部署**: 各服务可以独立开发、测试、部署
3. **服务发现**: 通过 Nacos 实现服务注册与发现
4. **统一网关**: 所有外部请求通过网关路由到对应服务

### 5.2 数据库设计原则

1. **范式设计**: 遵循数据库设计范式，减少数据冗余
2. **外键约束**: 建立合理的外键关系，保证数据完整性
3. **索引优化**: 为常用查询字段建立索引，提高查询性能
4. **时间字段**: 所有表都包含 created_at 和 updated_at 字段，自动填充

### 5.3 安全设计原则

1. **密码加密**: 使用 BCrypt 加密存储密码
2. **JWT 认证**: 无状态认证，Token 携带用户信息
3. **网关鉴权**: 在网关层统一验证 Token，减少后端重复鉴权
4. **白名单机制**: 登录、注册等接口不需要认证

### 5.4 代码复用设计

1. **公共模块**: petlife-common 包含所有服务共用的实体类、工具类、响应封装
2. **基类继承**: 所有实体类继承 BaseEntity，统一管理公共字段
3. **统一响应**: Result 类统一封装响应格式，前端处理更方便
4. **AOP 切面**: 通过 AOP 实现日志记录等横切关注点，不侵入业务代码

---

## 六、技术亮点

1. **微服务架构**: 采用 Spring Cloud Alibaba 实现微服务拆分，便于团队协作开发
2. **无状态认证**: JWT + 网关鉴权，无需在服务端保存会话信息
3. **AOP 操作日志**: 自动记录管理员操作，便于审计和追溯
4. **定时任务提醒**: 自动检查疫苗到期和预约订单，提升用户体验
5. **前后端分离**: 三个独立前端应用，各司其职
6. **响应式设计**: Vue 3 组合式 API，代码组织更灵活
7. **数据可视化**: ECharts 图表展示数据趋势，直观清晰
8. **代码复用**: 公共模块提取，减少重复代码